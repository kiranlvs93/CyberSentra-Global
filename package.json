{
  "name": "passless-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["server", "web"],
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "build": "npm run build --prefix server && npm run build --prefix web",
    "test": "npm run test --prefix server",
    "lint": "echo 'No root lint command'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
